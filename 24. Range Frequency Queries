from collections import Counter

def numOfFreq(arr, queries):
    result = []
    
    for i, j, x in queries:
        subarray = arr[i:j+1]  # Extract subarray
        freq = Counter(subarray)  # Count frequency
        result.append(freq.get(x, 0))  # Get frequency of x, default 0
    
    return result
